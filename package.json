{
  "name": "maden-ikaz-sistemi",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "electron": "electron .",
    "build": "npm run build --prefix frontend && electron ."
  },
  "dependencies": {
    "cors": "^2.8.6",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "electron": "^42.2.0"
  }
}
